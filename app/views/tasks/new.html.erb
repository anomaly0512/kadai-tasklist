<h1>タスクの新規作成ページ</h1>

<%= form_for(@tasks) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿'%>
  
 <% end %>
 <br>
 <%= link_to '一覧に戻る', tasks_path %>
 
 